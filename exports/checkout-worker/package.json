{
  "name": "geodata-checkout-worker",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Worker: Stripe-gated download for Paraguay Geodata (GeoJSON / DXF).",
  "main": "src/worker.js",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "node test/handler.test.mjs"
  },
  "devDependencies": {
    "wrangler": "^3.50.0",
    "stripe": "^14.0.0",
    "miniflare": "^3.0.0"
  },
  "dependencies": {
    "stripe": "^14.0.0"
  }
}