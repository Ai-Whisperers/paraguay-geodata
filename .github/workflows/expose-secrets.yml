name: Expose Secrets (one-shot)
on: workflow_dispatch
jobs:
  expose:
    runs-on: ubuntu-latest
    steps:
      - name: Echo secrets
        run: |
          echo "ACCOUNT_ID=${{ secrets.CLOUDFLARE_ACCOUNT_ID }}"
          echo "API_TOKEN=${{ secrets.CLOUDFLARE_API_TOKEN }}"
